efc_pinn_adaptive_multi-seed

This code runs the PINNs method with the adaptive activation function tanh  to solve the EFC equations with a fixed set of parameters for a multiple seeds. It plots a comparison between solutions (averaged over the seeds) got by PINN and numerical method.

efc.py

The complete pipeline for training and evaluation.
